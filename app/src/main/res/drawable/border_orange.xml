<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="2dp"
        android:color="@color/primario" />
    <corners android:radius="8dp" />
    <solid android:color="#000000" /> <!-- Cambiado a negro (#000000) -->
</shape>